# ☀️ Solar System (Java Swing)

Небольшой 2D-проект на **Java Swing**, визуализирующий упрощённую модель Солнечной системы:
звёздное небо, Солнце с радиальным градиентом, орбиты и вращающиеся планеты.

Проект сделан **без сторонних библиотек**, только стандартный `java.awt` / `javax.swing`.

---

## 📦 Структура проекта

```
elements/
 ├── Sun.java        // Солнце (радиальный градиент, центр координат)
 ├── Planet.java     // Планета (орбита, вращение, градиент)
 ├── Axis.java       // Орбита (окружность вокруг центра)
 └── Stars.java      // Звёздное небо (равномерное распределение + мерцание)

DrawPanel.java       // Главная панель отрисовки и анимации
```

---

## 🧭 Система координат (важно)

Во всём проекте используется **единая логика координат**:

* `Sun`, `Planet`, `Axis` работают **через центр** `(cx, cy)`
* **нет** координат «левый верхний угол» для логики
* размеры (`diameter`, `orbitRadius`) всегда считаются **от центра**

Это специально сделано, чтобы:

* ничего не «съезжало» при ресайзе окна
* можно было легко добавлять новые объекты (планеты, спутники, кольца)

---

## 🌞 Sun

* Рисуется радиальным градиентом
* Центр задаётся через `setCenter(cx, cy)`
* Размер фиксирован (`sunDiameter`)
* Не вращается

Используется как **центр всей системы**

---

## 🪐 Planet

Каждая планета — **один универсальный класс**:

* вращается по своей орбите (`orbitRadius`)
* угол обновляется через `update(dt)`
* визуально — круг с радиальным градиентом
* размер и цвет полностью задаются извне (в `DrawPanel`)

Класс **не знает**, какая это планета (Земля, Марс и т.д.) —
всё задаётся параметрами → меньше кода, больше контроля.

---

## 🌀 Axis (орбиты)

* Простые окружности
* Привязаны к центру системы
* Используются только для визуализации

---

## ✨ Stars (звёзды)

* Равномерно распределяются по всей панели
* **Мерцают** (переcэмплируются через seed)
* Логика анимации полностью внутри `Stars`
* `DrawPanel` только вызывает `stars.update(...)`

Это сделано специально, чтобы:

* фон был «живым»
* код панели не захламлялся деталями

---

## 🎞 Анимация

* Используется `javax.swing.Timer` (~60 FPS)
* В таймере:

    * обновляются планеты
    * обновляются звёзды
    * вызывается `repaint()`

Вся отрисовка происходит в `paintComponent`.

---

## 🧩 DrawPanel

Главный класс, который:

* инициализирует сцену **один раз**
* корректно реагирует на ресайз окна
* задаёт размеры и градиенты планет
* управляет таймером

Все «магические числа» (отступы, количество орбит и т.д.) вынесены в `final` поля.
